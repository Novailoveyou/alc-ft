import 'server-only'
import { Button } from '@/components/ui/button'

export const Filters = ({}) => {
  return (
    <section>
      <div>
        <h1>Filters</h1>
      </div>
    </section>
  )
}
